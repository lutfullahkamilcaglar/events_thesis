import 'package:flutter/material.dart';

class ProfileLockedView extends StatelessWidget {
  const ProfileLockedView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
